vsc (version control system)
uses the dependencies.repos file to download necessary files from the repositories

rosdep (ROS dependencies)
uses the ros archive to downlaod the necessay packages/libraries for running the programs